# JDBC driver

Place your approved IBM DB2 JDBC JAR here or configure its existing absolute path. IBM binaries are not included in the shared archive. Use the exact version already approved for your server/Java combination. No Python, npm, or Maven dependency is required at runtime.
